using System;
using System.Collections.Generic;
using System.Linq;
using System.Runtime.Serialization.Formatters.Binary;
using System.Text;
using System.Threading.Tasks;


namespace FinalTask
{
    //internal class SorterStream
    //{
    //    // поле класса 
    //    private string pathToFile;

    //    // метод класса
    //    public void sortStream(string pathToBinFile)
    //    {

    //        pathToFile = pathToBinFile;

    //        // на всякий случай сделаем конструкцию try / catch
           
    //            BinaryFormatter binaryFormatter = new BinaryFormatter();

    //            // используем директиву using
    //            using (var streamInfoStudents = new FileStream(pathToFile, FileMode.Open))
    //            {
    //                var students = (InfoStudents[])formatter.Deserialize(streamInfoStudents);
    //                foreach (var student in students)
    //                {
    //                    Console.WriteLine(student.Name +" " + student.Group +" "+ student.DateBith);
    //                }
    //            }         
    //    }

    //}
}
